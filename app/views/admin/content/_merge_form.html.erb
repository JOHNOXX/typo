<% if current_user.admin? %>
  <div class=''>
    <h4><%= _("Merge Articles") %></h4>
    <div class='class'>
      <label for="merge_with"><%= _("Article ID:") %>
        <input id='merge_with' type="text" value="" name="merge_with" />
        <input id='merge' type="submit" value="<%= _('Merge') %>" name="merge" />
      </label>
    </div>
  </div>
<% end %>

